(()=>{
 state.systemStage=state.systemStage||'FLUID';
 window.setSystemStage=s=>{state.systemStage=s;if(s==='WHIP'&&whipUnlocked())state.page='whipcurriculum';else if(s==='WEAPON'&&typeof weaponUnlocked==='function'&&weaponUnlocked())state.page='weapon';else if(s==='FLUID')state.page='learn';save();render()};
 window.whipUnlocked=()=>FLUID.levels[2].gate.every(x=>state.earned.includes(x));
 window.stageOverview=()=>{const wu=whipUnlocked(),pu=typeof weaponUnlocked==='function'&&weaponUnlocked();return `<div class="stage-switch"><button class="stage-fluid ${state.systemStage==='FLUID'?'active':''}" onclick="setSystemStage('FLUID')"><small>STAGE I</small><b>FLUID</b><span>Connect the Chain</span></button><button class="stage-whip ${state.systemStage==='WHIP'?'active':''} ${wu?'':'stage-locked'}" ${wu?`onclick="setSystemStage('WHIP')"`:''}><small>STAGE II</small><b>WHIP</b><span>${wu?'Accelerate the Chain':'Complete FLUID to unlock'}</span></button><button class="stage-weapon ${state.systemStage==='WEAPON'?'active':''} ${pu?'':'stage-locked'}" ${pu?`onclick="setSystemStage('WEAPON')"`:''}><small>STAGE III</small><b>WEAPON</b><span>${pu?'Apply the Chain':'Complete WHIP to unlock'}</span></button></div>`};
 window.whipManual=()=>`<div class="pagehead"><div><div class="eyebrow">STAGE II · PERFORMANCE LAYER</div><h1>WHIP</h1><p>Acceleration is earned from coordination. Speed is never used to hide poor structure.</p></div>${whipUnlocked()?'<button class="primary" onclick="enterWhip()">ENTER WHIP TRAINING</button>':''}</div>${stageOverview()}<section class="card wide"><div class="eyebrow">THE FIVE LAWS</div><div class="principle-grid">${WHIP.principles.map((p,i)=>`<article><strong>0${i+1}</strong><div><b>${p.name}</b><span>${p.desc}</span></div></article>`).join('')}</div></section>${WHIP.levels.map(l=>`<section class="card wide"><div class="eyebrow">${l.id} · ${l.name}</div><h3>${l.mission}</h3><div class="workout">${WHIP.sessions.filter(s=>s.level===l.id).map(s=>`<div class="move"><div class="move-icon">${s.id.split('-')[1]}</div><div class="move-copy"><small>${s.focus}</small><strong>${s.name}</strong><small>${s.chain}</small></div><span class="tag">${whipUnlocked()?'READY':'PREVIEW'}</span></div>`).join('')}</div></section>`).join('')}`;
 const originalLearn=learn;
 window.learn=()=>state.systemStage==='WHIP'?whipManual():`${stageOverview()}${originalLearn()}`;
 render();
})();