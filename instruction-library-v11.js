(()=>{
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 function card(c){const visual=window.FWW_VISUAL_RENDERER?FWW_VISUAL_RENDERER.sequence(c):`<div class="instruction-sequence">${c.frames.map((f,i)=>`<div class="instruction-frame"><span>${i+1}</span><b>${esc(f)}</b><small>FINAL ANATOMICAL RENDER PENDING</small></div>`).join('')}</div>`;return `<article class="card wide instruction-card"><div class="eyebrow">${esc(c.category)} · ${esc(c.range)}</div><h3>${esc(c.title)}</h3>${visual}<div class="instruction-grid"><div><small>HOW TO PERFORM</small><ol>${c.how.map(x=>`<li>${esc(x)}</li>`).join('')}</ol></div><div><small>KEY CUES</small><ul>${c.cues.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><div><small>COMMON ERRORS</small><ul>${c.errors.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div></div><div class="dash-line"><b>Regression</b><span>${esc(c.regression)}</span></div><div class="dash-line"><b>Progression</b><span>${esc(c.progression)}</span></div></article>`}
 window.renderInstructionLibrary=function(filter='ALL'){const all=FWW_INSTRUCTION_CARDS.cards,items=filter==='ALL'?all:all.filter(c=>c.category===filter),audit=window.FWW_VISUAL_RENDERER?FWW_VISUAL_RENDERER.audit():null;document.getElementById('app').innerHTML=`<div class="app"><div class="pagehead"><div><div class="eyebrow">MOVEMENT PASSPORT · INSTRUCTION</div><h1>Technique Library</h1><p>Every movement is taught as a sequence: position → chain → expression → recovery.</p>${audit?`<small class="visual-audit">VISUAL QA · ${audit.approved}/${audit.total} APPROVED · ${audit.pending} PENDING</small>`:''}</div></div><div class="actions">${['ALL','FITNESS','WEAPON','DRILL'].map(x=>`<button class="${filter===x?'primary':'secondary'}" onclick="renderInstructionLibrary('${x}')">${x}</button>`).join('')}</div><div class="workout">${items.map(card).join('')}</div></div>`;window.scrollTo(0,0)};
 if(window.AppRouter&&typeof AppRouter.register==='function')AppRouter.register('instruction',()=>renderInstructionLibrary());
})();