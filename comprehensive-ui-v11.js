(()=>{
 function h(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
 function currentWeek(){const n=Number(localStorage.getItem('fww-program-week-v11')||1);return Math.min(24,Math.max(1,n))}
 function setWeek(n){localStorage.setItem('fww-program-week-v11',String(Math.min(24,Math.max(1,n))));renderComprehensiveProgram()}
 window.openComprehensiveSession=function(id){const s=FWW_WORKOUTS.sessions[id];if(!s)return;document.getElementById('app').innerHTML=`<div class="app"><div class="pagehead"><div><div class="eyebrow">COMPREHENSIVE TRAINING</div><h1>${h(s.name)}</h1><p>${h(s.rounds)}</p></div></div><div class="workout">${s.items.map((x,i)=>`<article class="card wide"><div class="eyebrow">${String(i+1).padStart(2,'0')}</div><h3>${h(x.name)}</h3><p><b>${h(x.dose||'')}</b></p>${x.focus?`<p>${h(x.focus)}</p>`:''}${x.reg?`<p><small>REGRESSION</small><br>${h(x.reg)}</p>`:''}${x.prog?`<p><small>PROGRESSION</small><br>${h(x.prog)}</p>`:''}${x.rest?`<span class="tag">REST ${h(x.rest)}</span>`:''}</article>`).join('')}</div><div class="actions"><button class="secondary" onclick="renderComprehensiveProgram()">BACK TO PROGRAM</button></div></div>`;window.scrollTo(0,0)};
 window.renderComprehensiveProgram=function(){const week=currentWeek(),w=FWW_COMPREHENSIVE.buildWeek(week),p=FWW_COMPREHENSIVE.phases.find(x=>x.id===w.phase),ids=FWW_WORKOUTS.prescriptionForPhase(p.id),tests=FWW_WORKOUTS.tests[p.id]||[];document.getElementById('app').innerHTML=`<div class="app"><section class="hero command-hero"><div class="hero-copy"><div class="eyebrow">24-WEEK COMPLETE SYSTEM · WEEK ${week}</div><h1>${h(p.name)}</h1><p>${h(p.goal)}</p><div class="actions"><button class="secondary" onclick="setComprehensiveWeek(${week-1})" ${week===1?'disabled':''}>← WEEK ${Math.max(1,week-1)}</button><button class="primary" onclick="setComprehensiveWeek(${week+1})" ${week===24?'disabled':''}>WEEK ${Math.min(24,week+1)} →</button></div></div></section><div class="card wide"><div class="eyebrow">PHASE ${h(p.id)} · WEEKS ${h(p.weeks)}</div><h3>Training Sessions</h3><div class="program-blocks">${ids.map(id=>{const s=FWW_WORKOUTS.sessions[id];return `<article><small>${h(p.name)}</small><b>${h(s.name)}</b><span>${h(s.rounds)}</span><button class="primary full" onclick="openComprehensiveSession('${id}')">OPEN SESSION</button></article>`}).join('')}</div></div><div class="card wide"><div class="eyebrow">WEEKLY RHYTHM</div><h3>Seven-Day Structure</h3><div class="week-strip">${w.days.map(d=>`<button><b>${h(d.day)}</b><small>${h(d.name)}</small></button>`).join('')}</div></div><div class="card wide"><div class="eyebrow">COMPETENCY GATE</div><h3>${h(p.gate)}</h3>${tests.map(t=>`<div class="dash-line"><b>${h(t[0])}</b><span>${h(t[1])}</span></div>`).join('')}</div><div class="card wide"><div class="eyebrow">RANGE CONTINUITY</div><h3>No Dead Zones</h3><div class="range-map">${FWW_COMPREHENSIVE.ranges.map(r=>`<button><small>${h(r.id)}</small><b>${h(r.name)}</b><span>${h(r.jobs.join(' · '))}</span></button>`).join('')}</div><p>${h(FWW_COMPREHENSIVE.rules.grappling)}</p></div></div>`;window.scrollTo(0,0)};
 window.setComprehensiveWeek=setWeek;
 if(window.AppRouter&&typeof AppRouter.register==='function')AppRouter.register('completeprogram',renderComprehensiveProgram);
})();