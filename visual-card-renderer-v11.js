(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));window.FWW_VISUAL_RENDERER={asset(id){return FWW_VISUAL_PRODUCTION.assetPath(id)},frameFallback(card){return `<div class="instruction-sequence">${card.frames.map((f,i)=>`<div class="instruction-frame"><span>${i+1}</span><b>${esc(f)}</b><small>FINAL ANATOMICAL RENDER PENDING</small></div>`).join('')}</div>`},sequence(card){const status=FWW_INSTRUCTION_CARDS.visualManifest.status[card.id];if(status!=='approved')return this.frameFallback(card);return `<figure class="approved-technique-visual"><img src="${this.asset(card.id)}" alt="${esc(card.title)} instructional movement sequence" loading="lazy" onerror="this.closest('figure').outerHTML=FWW_VISUAL_RENDERER.frameFallback(FWW_INSTRUCTION_CARDS.cards.find(c=>c.id==='${card.id}'))"><figcaption>${esc(card.title)} · approved anatomical sequence</figcaption></figure>`},audit(){const cards=FWW_INSTRUCTION_CARDS.cards;return {total:cards.length,approved:cards.filter(c=>FWW_INSTRUCTION_CARDS.visualManifest.status[c.id]==='approved').length,pending:cards.filter(c=>FWW_INSTRUCTION_CARDS.visualManifest.status[c.id]==='pending-final-avatar-render').length,review:cards.filter(c=>FWW_INSTRUCTION_CARDS.visualManifest.status[c.id]==='generated-needs-review').length,rejected:cards.filter(c=>FWW_INSTRUCTION_CARDS.visualManifest.status[c.id]==='rejected').length}}};})();